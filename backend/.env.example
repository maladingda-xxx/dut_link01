# DeepSeek API key (chat only — DeepSeek has no embedding endpoint).
DEEPSEEK_API_KEY=

# PostgreSQL connection (asyncpg). Defaults to localhost; override as needed.
DUTLINK_DATABASE_URL=postgresql+asyncpg://dutlink:dutlink@localhost:5432/dutlink
